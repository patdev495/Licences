const images = {
    cyradar: require('./cyrada.svg').default,
    Licence: require('./licence.svg').default,
    Computer: require('./computer.svg').default,
    Client: require('./client.svg').default,
    Notification: require('./notification.svg').default,
};

export default images;
